node_modules
.env
.env.*
.DS_Store
coverage/

# Accumulated QC-subagent project memory (machine-written, ever-growing local
# state). Ignored by default like .claude.json; un-ignore deliberately if the
# team wants to share accumulated review context.
.claude/agent-memory/

# Personal Claude Code overrides (not shared)
.claude/settings.local.json
